<UserControl x:Class="Olf.GoldenHorse.Core.Views.Views.Tests.TestEditor.TestObjectEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="350">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"></RowDefinition>
            <RowDefinition Height="220"></RowDefinition>
            <RowDefinition Height="40"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel  Orientation="Vertical" Grid.Row="0">
            <Grid >
                <Button Height="30" Width="30" Margin="10,10,0,28" HorizontalAlignment="Left"/>
                <TextBlock Margin="50,10,20,10"  TextWrapping="Wrap" Text="Move the mouse to the test to be validated on screen. Press Shift+Ctrl+A to select the text."/>
            </Grid>
            <Grid>
                <Button Height="30" Width="30" Margin="10,10,0,28" HorizontalAlignment="Left"/>
                <TextBlock  Margin="50,10,20,10" TextWrapping="Wrap" Text="Selecting this will open the Visualizer view. Click a control to set its property to the validation text."></TextBlock>
            </Grid>
        </StackPanel>
        <Grid Grid.Row="1" Height="150">
              <TreeView ItemsSource="{Binding IDontKnow}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ></TreeView>
        </Grid>
        
     
    </Grid>
</UserControl>
