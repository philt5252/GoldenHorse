<UserControl x:Class="Olf.GoldenHorse.Core.Views.Views.Variables.VariableManager_View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Type" Width="80" ItemsSource="{Binding Type}" 
                                        SelectedItemBinding="{Binding SelectedType}"></DataGridComboBoxColumn>
                <DataGridTextColumn Header="Name" Width="120" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Header="Default Value" Width="150" Binding="{Binding DefaultValue}"></DataGridTextColumn>
                <DataGridTextColumn Header="Description" Width="150" Binding="{Binding Description}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
            
    </Grid>
</UserControl>
